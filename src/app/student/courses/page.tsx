// Formations de l'élève
export default function MyCoursesPage() {
  // Fetch côté serveur (Server Component) ou côté client
  // pour récupérer la liste de formations de l'élève
  return (
    <div>
      <h1>Mes Formations</h1>
      {/* Affiche la liste (table, cards, etc.) */}
    </div>
  );
}
